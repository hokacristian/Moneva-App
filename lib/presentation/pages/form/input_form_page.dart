// input_form_page.dart
import 'package:flutter/material.dart';

class InputFormPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Input Form')),
      body: const Center(child: Text('Halaman Input Form')),
    );
  }
}
