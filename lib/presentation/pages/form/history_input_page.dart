// history_form_page.dart
import 'package:flutter/material.dart';

class HistoryInputPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('History Form')),
      body: const Center(child: Text('Halaman History Form')),
    );
  }
}
