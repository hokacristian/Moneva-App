// dampak_page.dart
import 'package:flutter/material.dart';

class DampakPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dampak')),
      body: const Center(child: Text('Halaman Dampak')),
    );
  }
}
