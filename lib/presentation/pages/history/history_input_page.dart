import 'package:flutter/material.dart';

class HistoryInputPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('History Input Page')),
    );
  }
}
